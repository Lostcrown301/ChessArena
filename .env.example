# Root level environment file notice.
# The backend and frontend are independent applications with their own environment configurations.
# Copy backend/.env.example into backend/.env for backend configuration.
# Copy frontend/.env.example into frontend/.env for frontend configuration.
